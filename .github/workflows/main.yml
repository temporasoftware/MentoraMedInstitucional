name: MentoraMed Landing Page

on:
  push:
    branches: [main]
permissions:
  contents: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
      
    steps:
      - name: Exibir Docker Image
        run: |
          echo "🔧 Dockerhub Image: ${{ secrets.DOCKER_IMAGE }}"
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Gerar .env.production
        run: |
          cat <<EOF > .env.production
          EOF
      - name: Login no Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build da imagem Docker
        run: |
          docker build \
            -t ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE }}:latest .
      - name: Push da imagem
        run: docker push ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE }}:latest

      - name: Redeploy (Portainer /stacks/:id PUT)
        run: |
          set -euo pipefail
      
          # Vars vindas do seu workflow/env
          STACK_ID="8"
          ENDPOINT_ID="24"
      
          # Monta o JSON esperado pelo Portainer
          jq -n \
            --arg content "${{ secrets.PORTAINER_COMPOSE_CONTENT }}" \
            --argjson id "$STACK_ID" \
            --argjson prune false \
            --argjson pull true \
            '{
              id: $id,
              StackFileContent: $content,
              Env: [],
              Prune: $prune,
              PullImage: $pull
            }' \
          | curl -fsS --retry 5 --retry-all-errors --max-time 60 \
              -H "X-API-Key: ${{ secrets.PORTAINER_TOKEN }}" \
              -H "Content-Type: application/json" \
              -X PUT "https://portainer.nileo.com.br/api/stacks/${STACK_ID}?endpointId=${ENDPOINT_ID}" \
              --data-binary @-
